# Inspired heavily by the following workflows
# https://github.com/arichornlover/uYouEnhanced/blob/main/.github/workflows/buildapp.yml
# https://github.com/ISnackable/YTCubePlus/blob/main/.github/workflows/Build.yml
# https://github.com/BandarHL/BHTwitter/actions/workflows/build.yml

name: Build and Package Sparkle

on:
    workflow_dispatch:
        inputs:
            decrypted_instagram_url:
                description: "The direct URL to the decrypted Instagram IPA"
                default: ""
                required: true
                type: string
            upload_artifact:
                description: "Upload Artifact"
                default: true
                required: false
                type: boolean
            with_flex:
                description: "Include libflex"
                default: false
                required: false
                type: boolean
            for_sidestore:
                description: "Build for SideStore (Strips Extensions)"
                default: false
                required: false
                type: boolean
            bundle_id:
                description: "Bundle ID"
                default: ""
                required: false
                type: string
  
concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true
jobs:
    build:
        name: Build Sparkle
        runs-on: macos-latest
        permissions:
            contents: write

        steps:
            - name: Checkout Main
              uses: actions/checkout@v4
              with:
                path: main
                submodules: recursive

            - name: Install Dependencies
              run: brew install ldid dpkg make autoconf automake cmake libtool meson nasm ninja pkgconf wget yasm

            - name: Set PATH environment variable
              run: echo "$(brew --prefix make)/libexec/gnubin" >> $GITHUB_PATH 

            - name: Setup Theos
              uses: actions/checkout@v4
              with:
                repository: theos/theos
                # Pinned to a known-good commit. theos master tracks the latest
                # Logos, and commit 777925d (#114) regressed bare %orig parsing
                # (single-line `{ %orig; }`, `%orig]`, etc.). This commit references
                # Logos a623700, the last version that builds this project.
                ref: 405e5c6ddb288615c030eeaedc6b265aae748285
                path: ${{ github.workspace }}/theos
                submodules: recursive
        
            - name: SDK Caching
              id: SDK
              uses: actions/cache@v4
              env:
                cache-name: iPhoneOS16.2.sdk
              with:
                path: ${{ github.workspace }}/theos/sdks/
                key: ${{ env.cache-name }}
                restore-keys: ${{ env.cache-name }}

            - name: Download iOS SDK
              if: steps.SDK.outputs.cache-hit != 'true'
              run: |
                git clone --quiet -n --depth=1 --filter=tree:0 https://github.com/xybp888/iOS-SDKs/
                cd iOS-SDKs
                git sparse-checkout set --no-cone iPhoneOS16.2.sdk
                git checkout
                mv *.sdk $THEOS/sdks
              env:
                THEOS: ${{ github.workspace }}/theos

            - name: Prepare Instagram IPA
              run: |
                cd main

                mkdir -p packages
                wget "$Instagram_URL" --no-verbose -O packages/com.burbn.instagram.ipa

                ls -la packages
                
              env:
                THEOS: ${{ github.workspace }}/theos
                Instagram_URL: ${{ inputs.decrypted_instagram_url }}

            - name: Get Sparkle Version
              id: sparkle_version
              run: |
                SPARKLE_VERSION=$(awk '/Version:/ {print $2}' main/control)

                echo "SPARKLE_VERSION=${SPARKLE_VERSION}" >> "$GITHUB_ENV"
                echo "version=${SPARKLE_VERSION}" >> "$GITHUB_OUTPUT"

            - name: Cache FFmpeg frameworks
              id: ffmpeg_cache
              uses: actions/cache@v4
              with:
                path: |
                  main/modules/ffmpegkit
                  main/.cache/ffmpegkit
                key: ffmpegkit-${{ runner.os }}-${{ hashFiles('main/fetch-ffmpegkit.sh') }}

            - name: Prepare FFmpeg frameworks
              run: |
                cd main
                chmod +x ./fetch-ffmpegkit.sh
                ./fetch-ffmpegkit.sh
            
            - name: Build Sparkle tweak for sideloading (as IPA)
              run: |
                pip install --force-reinstall https://github.com/asdfzxcvbn/pyzule-rw/archive/main.zip
                
                cd main
                curl -Lo ipapatch https://github.com/asdfzxcvbn/ipapatch/releases/download/v2.1.3/ipapatch.macos-arm64
                chmod +x ipapatch
                export PATH=.:$PATH
                ls -la

                FLAGS="--release"
                if [ "${{ inputs.for_sidestore }}" = "true" ]; then
                  FLAGS="--sidestore"
                fi
                if [ "${{ inputs.with_flex }}" = "true" ]; then
                  FLAGS="$FLAGS --flex"
                fi
                if [ -n "${{ inputs.bundle_id }}" ]; then
                  FLAGS="$FLAGS --bundle-id ${{ inputs.bundle_id }}"
                fi
                ./build.sh ipa $FLAGS
                ls -la packages
                
              env:
                THEOS: ${{ github.workspace }}/theos

            - name: Pass package name to upload action
              id: package_name
              run: |
                # build.sh already produces Sparkle[_<flags>]_v<version>_IG_v<ig version>.ipa
                IPA_PATH="$(ls -t main/packages/Sparkle_*_IG_v*.ipa | head -n1)"
                echo "package=$(basename "$IPA_PATH")" >> "$GITHUB_OUTPUT"
                echo "path=${IPA_PATH}" >> "$GITHUB_OUTPUT"

            - name: Upload Artifact
              if: ${{ inputs.upload_artifact }}
              uses: actions/upload-artifact@v4
              with:
                name: ${{ steps.package_name.outputs.package }}
                path: ${{ github.workspace }}/${{ steps.package_name.outputs.path }}
                if-no-files-found: error

            - name: Create Release
              uses: softprops/action-gh-release@v2.0.6
              with:
                name: Sparkle v${{ steps.sparkle_version.outputs.version }}
                files: ${{ github.workspace }}/main/packages/Sparkle_*_IG_v*.ipa
                draft: true

            - name: Output Release URL
              run: |
                echo "::notice::Release available at: https://github.com/${{ github.repository }}/releases"
